server.port: 8888

spring:
  application.name: cloud-config-server

  cloud.config.server:
    git:
      uri: https://github.com/daggerok/spring-restoroud-config
      basedir: build/repo
    bootstrap: true
